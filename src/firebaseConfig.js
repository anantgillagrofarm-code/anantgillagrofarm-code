// src/firebaseConfig.js
// Firebase temporarily disabled.
// This stub avoids runtime errors if Firebase config is missing.
// Later, when you create a Firebase project, replace these values.

export const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: ''
};

// Flag to check if Firebase should be used
export const FIREBASE_ENABLED = false;
